{
  "name": "@volley/crucible-deploy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "crucible-deploy": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest",
    "test:run": "vitest --run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "chalk": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0",
    "@types/node": "^22.0.0"
  }
}
